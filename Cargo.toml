[package]
name = "rexile"
version = "0.1.0"
edition = "2021"
authors = ["You <you@example.com>"]
license = "MIT OR Apache-2.0"
description = "ReXile — a fast, ergonomic replacement for regex usage in the GRL parser ecosystem."

[lib]
name = "rexile"
path = "src/lib.rs"

[dependencies]
regex = "1"
once_cell = "1"

[dev-dependencies]
criterion = "0.5"

[workspace]
